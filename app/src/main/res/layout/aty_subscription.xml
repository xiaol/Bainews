<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f6f6f6">

    <TextView
        android:id="@+id/title_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="55dp"
        android:text="根据你的爱好为你推荐"
        android:textColor="#131313"
        android:textSize="@dimen/new_font2" />

    <TextView
        android:id="@+id/start_imageView"
        android:layout_width="match_parent"
        android:layout_height="53dp"
        android:layout_alignParentBottom="true"
        android:background="@color/news_subscription_color1"
        android:gravity="center"
        android:text="开始体验"
        android:textColor="#ffffff"
        android:textSize="16sp" />

    <GridView
        android:id="@+id/subscription_gridView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/start_imageView"
        android:layout_below="@+id/title_view"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:layout_marginTop="32dp"
        android:overScrollMode="never"
        android:numColumns="3"
        android:scrollbars="none"
        android:clipToPadding="false"
        android:paddingBottom="55dp"
        android:verticalSpacing="37dp" />


</RelativeLayout>
